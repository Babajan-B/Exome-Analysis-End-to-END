╔════════════════════════════════════════════════════════════════╗
║          📋 COPY & PASTE THESE COMMANDS IN JARVIS LAB         ║
╚════════════════════════════════════════════════════════════════╝

ANNOVAR IS INSTALLED ✅ - Now let's annotate your VCF files!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 OPTION 1: AUTOMATIC (RECOMMENDED)

Copy and paste this in Jarvis Lab terminal:

┌────────────────────────────────────────────────────────────────┐
│ cd ~/NGS                                                       │
│ bash annotate_existing_vcfs.sh                                 │
└────────────────────────────────────────────────────────────────┘

This will:
  ✓ Find all VCF files automatically
  ✓ Show you what will be annotated
  ✓ Annotate them with ANNOVAR
  ✓ Take ~5-10 min per VCF

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 OPTION 2: MANUAL (STEP BY STEP)

STEP 1: Find your VCF files
┌────────────────────────────────────────────────────────────────┐
│ cd ~/NGS                                                       │
│ find results -name "*.vcf" -type f | grep -v ".vcf.gz"        │
└────────────────────────────────────────────────────────────────┘

STEP 2: List your sample IDs
┌────────────────────────────────────────────────────────────────┐
│ ls -la results/                                                │
└────────────────────────────────────────────────────────────────┘

STEP 3: Annotate first VCF (replace SAMPLE_ID with actual ID)
┌────────────────────────────────────────────────────────────────┐
│ SAMPLE_ID="your-sample-id-here"                                │
│ mkdir -p results/$SAMPLE_ID/annovar                            │
│ bash annovar_helper.sh \                                       │
│     results/$SAMPLE_ID/filtered/filtered_variants.vcf \        │
│     results/$SAMPLE_ID/annovar/annotated                       │
└────────────────────────────────────────────────────────────────┘

STEP 4: Annotate second VCF (if you have another)
┌────────────────────────────────────────────────────────────────┐
│ SAMPLE_ID="your-second-sample-id-here"                         │
│ mkdir -p results/$SAMPLE_ID/annovar                            │
│ bash annovar_helper.sh \                                       │
│     results/$SAMPLE_ID/filtered/filtered_variants.vcf \        │
│     results/$SAMPLE_ID/annovar/annotated                       │
└────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 CHECK RESULTS

After annotation completes:
┌────────────────────────────────────────────────────────────────┐
│ # List all annotated files                                    │
│ find results -name "*.hg19_multianno.*"                        │
│                                                                │
│ # Count variants in each                                      │
│ for f in results/*/annovar/*.hg19_multianno.vcf; do           │
│     echo "=== $f ==="                                         │
│     grep -v "^#" "$f" | wc -l                                 │
│ done                                                           │
└────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📥 DOWNLOAD RESULTS

Your annotated files will be in:
  results/SAMPLE_ID/annovar/annotated.hg19_multianno.vcf
  results/SAMPLE_ID/annovar/annotated.hg19_multianno.txt

Download using JupyterLab:
  1. Navigate to results/SAMPLE_ID/annovar/
  2. Right-click files → Download

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 QUICK PATHOGENIC VARIANT CHECK

After annotation:
┌────────────────────────────────────────────────────────────────┐
│ # Count pathogenic variants                                   │
│ grep "Pathogenic" results/*/annovar/*.txt | wc -l             │
│                                                                │
│ # List genes with pathogenic variants                         │
│ grep "Pathogenic" results/*/annovar/*.txt | cut -f7 | sort -u │
└────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 MORE INFO

See: JARVIS_LAB_COMMANDS.md for detailed instructions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
